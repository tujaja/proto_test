<div id="remote_form">
<% remote_form_for @content, :url => url do |f| %>
  <%= render :partial => "form_items", :locals => { :f => f } %>
  <p class="buttons"><%= f.submit action_name %> </p>
<% end %>
</div>
